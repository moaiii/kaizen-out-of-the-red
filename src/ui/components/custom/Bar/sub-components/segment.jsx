import React from 'react';

export default ({ data }) => {

  let style = {
    height: "100%",
    width: "20px"
  };

  return(
    <div className={`Segment`} style={style}>
      Segment
    </div>
  )
}